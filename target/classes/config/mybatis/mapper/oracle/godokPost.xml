<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sql.godokPost">

	<select id="selectAll" parameterType="int" resultType="PostVO">
		select * from godokPost where threadNo=#{no}
		order by postno asc
	</select>
	
	<insert id="insertPost" parameterType="PostVO">
		<selectKey keyProperty="no" order="BEFORE" resultType="int">
			select godokPost_seq.nextval from dual
		</selectKey>
		insert into godokPost (no, threadNo, pwd, postno )
		values(#{no}, #{threadNo}, #{pwd}, 
			(select #{postNo}+1
			from godokPost
			Where threadNo = #{threadNo} and postno = #{postNo}))
	</insert>
	<!-- insert 서브쿼리 
	insert 하는 postno는 
	1.기존에 입력된 같은 스레드의 postno에 비해
	2. 1의 최대값보다 1이 커야한다. -->


</mapper>









